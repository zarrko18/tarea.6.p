router = new Router('/');

function view(filename,data,content="body") {
  return function() {
	fetch(filename)
	  .then((response)=>response.text())
	  .then((template)=>{
		var rendered = Mustache.render(template,data);
		document.getElementById(content).innerHTML=rendered;
		router.updatePageLinks();
	 });
  };
};